# Optional — enables LLM-assisted extraction (Tier 1).
# Add as a GitHub repository secret named OPENROUTER_API_KEY.
# Without it the pipeline runs Tier 0 deterministic extraction.
OPENROUTER_API_KEY=
